import pygame

class EnemyData:
    def __init__(self, name, hp, moves):
        self.name = name
        self.hp = hp
        self.moves = moves

from test_cmbt import tackle, bite, solidify

slime = EnemyData(
    name='Slime',
    hp=30,
    moves=[tackle, bite, solidify]
)
