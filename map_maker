import pygame, random
from test_plat import draw_platform, Enemy
from enemy_data import slime

pygame.init()
pygame.mouse.set_visible(False)

SCREEN_WIDTH = 2000
SCREEN_HEIGHT = 600

dragging = False
start_pos = (0, 0)
current_pos = (0, 0)
saved_rects = []

font = pygame.font.Font(None, 22)

screen = pygame.display.set_mode((SCREEN_WIDTH, SCREEN_HEIGHT))
pygame.display.set_caption("Map Maker / Full Level View")

clock = pygame.time.Clock()

# -----------------------------
# Load Image Assets
p1 = 45
p2 = 35
bg_img = pygame.image.load("backgroundImage.png")
bg_img = pygame.transform.scale(bg_img, (2000, 600))
grass_img = pygame.image.load("GroundTop2.png")
grass_img = pygame.transform.scale(grass_img, (111, 120))
spawn_img = pygame.image.load("SpawnPointBed.png")
spawn_img = pygame.transform.scale(spawn_img, (32, 32))
fence1_img = pygame.image.load("Fence2.png")
fence1_img = pygame.transform.scale(fence1_img, (32, 32))
fencef_img = pygame.image.load("FenceFloor.png")
fencef_img = pygame.transform.scale(fencef_img, (32, 16))
tree1_img = pygame.image.load("Tree1.png")
tree1_img = pygame.transform.scale(tree1_img, (93, 115))
tree2_img = pygame.image.load("Tree2.png")
tree2_img = pygame.transform.scale(tree2_img, (99, 118))
tree3_img = pygame.image.load("Tree3.png")
tree3_img = pygame.transform.scale(tree3_img, (115, 100))
bush1img = pygame.image.load("Bush1.png")
bush1img = pygame.transform.scale(bush1img, (p1, p2))
bush2img = pygame.image.load("Bush2.png")
bush2img = pygame.transform.scale(bush2img, (p1, p2))
bush3img = pygame.image.load("Bush3.png")
bush3img = pygame.transform.scale(bush3img, (p1, p2))
enemy_frames = [pygame.transform.scale(pygame.image.load(f"RedEnemy{i}.png").convert_alpha(), (22, 30)) for i in
                    range(1, 5)]

cloud_images = [
    pygame.image.load("Cloud1.png"),
    pygame.image.load("Cloud2.png"),
    pygame.image.load("Cloud3.png"),
    pygame.image.load("Cloud4.png"),
    pygame.image.load("Cloud5.png"),
    pygame.image.load("Cloud6.png"),
    pygame.image.load("Cloud7.png"),
    pygame.image.load("Cloud8.png"),

]

bush_images = [
    bush1img,
    bush2img,
    bush3img,
]

bush = []

clouds = []

# TILE (scaled once, used everywhere)
bg_tile_original = pygame.image.load("backgroundTile.png")

TILE_SCALE = 0.1  # keep texture size normal (IMPORTANT FIX)

base_w = bg_tile_original.get_width()
base_h = bg_tile_original.get_height()

tile_w = int(base_w * TILE_SCALE)
tile_h = int(base_h * TILE_SCALE)

bg_tile_img = pygame.transform.scale(bg_tile_original, (tile_w, tile_h))


def load_level(level):
    if level == 0:
        background_rects = [
            pygame.Rect(866, 168, 107, 58),
            pygame.Rect(873, 225, 98, 13),
            pygame.Rect(202, 281, 60, 111),
            pygame.Rect(257, 290, 27, 32),
            pygame.Rect(252, 313, 18, 39),
            pygame.Rect(262, 344, 18, 20),
            pygame.Rect(258, 358, 34, 25),
            pygame.Rect(176, 271, 30, 32),
            pygame.Rect(185, 300, 25, 24),
            pygame.Rect(194, 319, 16, 24),
            pygame.Rect(183, 355, 29, 28),
            pygame.Rect(199, 340, 12, 26),
            pygame.Rect(193, 346, 15, 21),
            pygame.Rect(522, 245, 115, 44),
            pygame.Rect(537, 287, 98, 28),
            pygame.Rect(563, 313, 71, 48),
            pygame.Rect(548, 305, 26, 26),
            pygame.Rect(529, 279, 16, 20),
            pygame.Rect(548, 342, 23, 19),
            pygame.Rect(626, 264, 23, 26),
            pygame.Rect(613, 282, 27, 32),
            pygame.Rect(618, 310, 28, 26),
            pygame.Rect(629, 329, 31, 22),
            pygame.Rect(556, 323, 10, 25),
        ]

        platforms = [
            pygame.Rect(0, 308, 135, 300),
            pygame.Rect(135, 345, 53, 260),
            pygame.Rect(187, 375, 269, 230),
            pygame.Rect(544, 348, 145, 32),
            pygame.Rect(689, 364, 53, 240),
            pygame.Rect(171, 258, 45, 17),
            pygame.Rect(200, 268, 86, 28),
            pygame.Rect(515, 214, 118, 41),
            pygame.Rect(578, 242, 75, 27),
            pygame.Rect(745, 242, 54, 31),
            pygame.Rect(860, 153, 117, 30),
            pygame.Rect(886, 229, 25, 15),
            pygame.Rect(909, 219, 76, 25),
            pygame.Rect(809, 395, 58, 246),
            pygame.Rect(865, 365, 147, 255),
        ]
        fences = [
            pygame.Rect(878, 307 - 32, 73, 8),
            pygame.Rect(794, 267 - 30, 57, 6),
            pygame.Rect(878 + 32, 307 - 32, 73, 8),
            pygame.Rect(797 + 26, 267 - 30, 57, 6),
            pygame.Rect(285, 258, 45, 17),
            pygame.Rect(285 + 32, 258, 45, 17),

            pygame.Rect(408, 258 - 26, 45, 17),
            pygame.Rect(408 + 32, 258 - 26, 45, 17),

        ]
        fenceHBox = [
            pygame.Rect(285, 287, 66, 6),
            pygame.Rect(406, 283 - 21, 68, 9),

            pygame.Rect(797, 267, 57, 6),
            pygame.Rect(878, 307 - 3, 72 - 8, 7),
        ]

        tree1 = [
            pygame.Rect(200, 240 - 85, 93, 115)
        ]
        tree2 = [
            pygame.Rect(521, 97 + 3, 100, 118),
        ]
        tree3 = [
            pygame.Rect(866, 59 - 3, 103, 96 + 3),
        ]

        enemies = [
            Enemy(525, 182, 80, slime, 'slime_1', enemy_frames),
            Enemy(220, 235, 35, slime, 'slime_2', enemy_frames)
        ]

        def generate_random_positions(min_x, max_x, min_y, max_y, min_distance, amount):
            generated_bushes = []

            used_x_positions = []

            for _ in range(amount):
                x = random.randint(min_x, max_x)
                y = random.randint(min_y, max_y)

                while any(abs(x - used_x) <= min_distance for used_x in used_x_positions):
                    x = random.randint(min_x, max_x)

                used_x_positions.append(x)

                generated_bushes.append({
                    "rect": pygame.Rect(x, y, 40, 21),
                    "img": random.choice(bush_images)
                })

            return generated_bushes

        bushes = []

        # platform 1 bushes
        bushes += generate_random_positions(
            187,
            455 - 40,
            353,
            354,
            40,
            3
        )

        # platform 2 bushes
        bushes += generate_random_positions(
            173,
            175,
            240,
            240,
            40,
            1
        )

        # platform 3 bushes
        bushes += generate_random_positions(
            582,
            652 - 40,
            222,
            222,
            40,
            1
        )

        bushes += generate_random_positions(
            545,
            686 - 40,
            326,
            326,
            40,
            1
        )

        bushes += generate_random_positions(
            867,
            1011 - 40,
            343,
            343,
            55,
            2
        )

        bushes += generate_random_positions(
            912,
            983 - 40,
            197,
            197,
            40,
            1
        )
        # fences = [
        #     pygame.Rect(285, 279, 57, 10),
        #     # pygame.Rect(342, 276, 64, 17),  # box
        #     pygame.Rect(406, 283, 68, 9),
        #     pygame.Rect(797, 267, 57, 6),
        #     pygame.Rect(878, 307, 72, 7),
        # ]

        spawnpoints = [pygame.Rect(15, 286+2, 10, 3)]

    else:
        background_rects = []
        platforms = []
        spawnpoints = []

    return (
        platforms,
        spawnpoints,
        background_rects,
        tree1,
        tree2,
        tree3,
        bushes,
        fences,
        fenceHBox,
        enemies
    ) # fences


LEVEL = 0
platforms, spawnpoints, background_rects, tree1, tree2, tree3, bushes, fences, fenceHBox, enemies = load_level(LEVEL) #fences


# -----------------------------
# MAIN LOOP
# -----------------------------
running = True
while running:
    clock.tick(60)

    for event in pygame.event.get():
        if event.type == pygame.QUIT:
            running = False

        if event.type == pygame.MOUSEBUTTONDOWN:
            if event.button == 1:
                dragging = True
                start_pos = pygame.mouse.get_pos()

        if event.type == pygame.MOUSEBUTTONUP:
            if event.button == 1:
                dragging = False

            if event.button == 3:
                x1, y1 = start_pos
                x2, y2 = current_pos

                rect = pygame.Rect(
                    min(x1, x2),
                    min(y1, y2),
                    abs(x2 - x1),
                    abs(y2 - y1)
                )

                saved_rects.append(rect)
                print(f"pygame.Rect({rect.x}, {rect.y}, {rect.width}, {rect.height}),")

        if event.type == pygame.MOUSEMOTION:
            current_pos = pygame.mouse.get_pos()

    # -----------------------------
    # DRAW BACKGROUND IMAGE
    # -----------------------------
    screen.blit(bg_img, (0, 0))

    # -----------------------------
    # TILE BACKGROUND (FIXED)
    # -----------------------------
    for rect in background_rects:
        # Create a surface exactly the size of the rect
        surf = pygame.Surface((rect.width, rect.height), pygame.SRCALPHA)

        # Tile inside this surface (starts at 0,0 now)
        for x in range(0, rect.width, tile_w):
            for y in range(0, rect.height, tile_h):
                draw_w = min(tile_w, rect.width - x)
                draw_h = min(tile_h, rect.height - y)

                tile = bg_tile_img.subsurface((0, 0, draw_w, draw_h))
                surf.blit(tile, (x, y))

        # 🔵 OPTIONAL: Rounded edges (crop corners)
        radius = 6
        mask = pygame.Surface((rect.width, rect.height), pygame.SRCALPHA)
        pygame.draw.rect(mask, (255, 255, 255),
                         (0, 0, rect.width, rect.height),
                         border_radius=radius)

        surf.blit(mask, (0, 0), special_flags=pygame.BLEND_RGBA_MULT)

        # Draw final result to screen
        screen.blit(surf, (rect.x, rect.y))

    # -----------------------------
    # PLATFORMS
    # -----------------------------
    for br in background_rects:
        draw_platform(screen, bg_tile_img, br)
    for p in platforms:
        draw_platform(screen, grass_img, p)
    for t1 in tree1:
        draw_platform(screen, tree1_img, t1)
    for t2 in tree2:
        draw_platform(screen, tree2_img, t2)
    for t3 in tree3:
        draw_platform(screen, tree3_img, t3)
    for s in spawnpoints:
        screen.blit(spawn_img, (s.x, s.y))
    for f in fences:
        screen.blit(fence1_img, (f.x, f.y))
    for b in bushes:
        draw_platform(screen, b["img"], b["rect"])
    for fh in fenceHBox:
        draw_platform(screen, fencef_img, fh)
    for e in enemies: e.draw(screen, 0)

    # -----------------------------
    # UI / DEBUG
    # -----------------------------
    mx, my = pygame.mouse.get_pos()

    pygame.draw.line(screen, (168, 101, 201), (mx - 10, my), (mx + 10, my), 1)
    pygame.draw.line(screen, (168, 101, 201), (mx, my - 10), (mx, my + 10), 1)

    coords = font.render(f"{mx},{my}", True, (255, 255, 255))
    screen.blit(coords, (10, SCREEN_HEIGHT - 25))

    for rect in saved_rects:
        pygame.draw.rect(screen, (0, 255, 0), rect, 2)

        text = font.render(
            f"X:{rect.x}, Y:{rect.y} | W:{rect.width}, H:{rect.height}",
            True,
            (0, 255, 0)
        )

        screen.blit(text, (rect.x, rect.y - 20))

    if dragging:
        x1, y1 = start_pos
        x2, y2 = current_pos

        preview_rect = pygame.Rect(
            min(x1, x2),
            min(y1, y2),
            abs(x2 - x1),
            abs(y2 - y1)
        )

        pygame.draw.rect(screen, (255, 255, 255), preview_rect, 1)

    pygame.display.flip()

pygame.quit()
